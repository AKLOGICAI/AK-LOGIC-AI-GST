import React from 'react';

/**
 * OfflineBanner — Silent background sync mode (zero UI intrusion or banners).
 */
export const OfflineBanner: React.FC = () => {
  return null;
};

export default OfflineBanner;
